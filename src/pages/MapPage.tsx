import MapCanvas from '../components/map/MapCanvas';

const MapPage = () => {
    return (
        <div className="map-page">
            <MapCanvas />
        </div>
    );
};

export default MapPage;
